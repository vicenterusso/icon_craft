import 'package:flutter/material.dart';

class IconBorder {
  const IconBorder({
    this.width,
    this.color = Colors.white,
  });

  /// Border's width
  final double? width;

  /// Border's color
  final Color color;
}
